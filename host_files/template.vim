" File that sets the global variables needed to establish a mysql client
" connection.
let g:mysql_hostfile="{hostfile}"
let g:mysql_host="{host}"
let g:mysql_user="{user}"
let g:mysql_password="{password}"
